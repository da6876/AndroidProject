<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="oma.soc.bd.com.omanmotorassistance" >
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@drawable/ic_logos"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar" >

        <activity android:name=".PhoneVerifyCode"
            android:label="Phone Verify Code"
            android:theme="@style/AppTheme"  />
        <activity
            android:name=".ServicePage"
            android:label="Service Page"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AboutPage"
            android:label="@string/title_activity_about_page"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar" />
        <activity
            android:name=".ProfilePage"
            android:label="Profile"
            android:theme="@style/AppTheme" />
        <activity android:name=".FastPage"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomePage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".welcome.WelcomePage" />
        <activity
            android:name=".LogInOption"
            android:label="Log In Option"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LogInPage"
            android:label="Log In Here"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".RegistrationPage"
            android:label="Registration"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PhoneNumberVerify"
            android:label="Phone Verify"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".map.MapsPage"
            android:label="Service Car Status"
            android:theme="@style/AppTheme" ></activity>
    </application>

</manifest>